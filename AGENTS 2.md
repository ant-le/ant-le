# Development

For this project, I use test drived development so every 
aspect of the code should have according tests.

When building a new feature, first a failing test for that
given feature should be implemented. After that, you can start
implementing the respective feature

## Testing
Please run `npm run build` at the end to check if the solution works 
correctly.
